export const getDefaultBannerHeader = (agentName: string) => {
  return `Hi! I am ${agentName}`;
};

export const getDefaultBannerSubHeader = (orgName: string) => {
  return `I am an expert on all things ${orgName}. How can I help you today?`;
};
