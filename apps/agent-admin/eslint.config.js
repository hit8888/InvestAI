import baseConfig from '../../eslint.config.base.js';

export default baseConfig;
// Add any agent-admin specific overrides if needed:
/*
export default tseslint.config(
  { ignores: ['dist'] },
  baseConfig,
  {
    // agent-admin specific rules or overrides
    rules: {
      // ... 
    }
  }
);
*/
