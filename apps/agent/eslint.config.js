import baseConfig from '../../eslint.config.base.js';

export default baseConfig;
// Add any agent-specific overrides if needed:
/*
export default tseslint.config(
  { ignores: ['dist'] },
  baseConfig,
  {
    // agent-specific rules or overrides
    rules: {
      // ... 
    }
  }
);
*/
